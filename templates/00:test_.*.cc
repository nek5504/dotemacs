/**
 * @file `(file-name-nondirectory (buffer-file-name))`
 * @brief ${1:<describe-brief-here>}
 */

/* ---- Include Files ------------------------------------------------------- */
#include "`(substring (file-name-sans-extension (file-name-nondirectory buffer-file-name)) 5)`.h"

#include <gmock/gmock.h>
#include <gtest/gtest.h>

/* ---- Private Constants and Types ----------------------------------------- */

namespace {

/* ---- Private Variables --------------------------------------------------- */

/* ---- Private Function Prototypes ----------------------------------------- */

} // namespace

/* ---- Functions ----------------------------------------------------------- */

$0

namespace {

/* ---- Private Functions --------------------------------------------------- */

} // namespace

